{
  "name": "prompt-perfect",
  "version": "1.0.0",
  "description": "A guided, multi-stage console designed to help users construct effective prompts for AI models.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm run build && node server.js",
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --jsx=automatic"
  },
  "dependencies": {
    "express": "^4.19.2",
    "esbuild": "^0.21.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}